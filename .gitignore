# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (keep these secret!)
.env
.env.*
!.env.example
.env.local
.env*.local

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Android release signing — NEVER commit these (back them up safely instead)
android/keystore.properties
*.keystore
*.jks

# TypeScript incremental build cache
*.tsbuildinfo

# Vercel CLI linking metadata (project/org ids) — not source
.vercel/

# IDE / editor project config
.idea/
.vscode/

# Android build outputs / local machine config (never share)
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
*.apk
*.aab

# Local source archives (never commit — too large for GitHub)
Motiv Source New.zip
*.zip

# Local scratch artifact
.rt_orig.json
