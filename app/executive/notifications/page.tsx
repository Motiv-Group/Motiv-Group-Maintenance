// Notifications are role-agnostic (the page reads /api/notifications for the
// current user). Reuse the regional implementation for executives.
export { /* @next-codemod-error `default` export is re-exported. Check if this component uses `params` or `searchParams`*/
default } from '@/app/regional/notifications/page'
