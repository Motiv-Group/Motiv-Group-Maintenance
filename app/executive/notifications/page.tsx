// Notifications are role-agnostic (the page reads /api/notifications for the
// current user). Reuse the regional implementation for executives.
export { default } from '@/app/regional/notifications/page'
