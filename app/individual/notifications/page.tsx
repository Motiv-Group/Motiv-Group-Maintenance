// The notifications page is user-scoped (via /api/notifications), so the Individual
// area reuses the same screen as the store-manager view.
export { /* @next-codemod-error `default` export is re-exported. Check if this component uses `params` or `searchParams`*/
default } from '@/app/client/notifications/page'
